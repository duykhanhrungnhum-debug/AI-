[project]
name = "ai-agent"
version = "0.1.0"
description = "Verification-first autonomous AI Agent foundation"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
