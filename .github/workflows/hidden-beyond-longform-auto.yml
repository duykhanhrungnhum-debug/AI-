name: Hidden Beyond Bot1 Archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "BOT1_ARCHIVED"
          echo "Production execution is disabled. Code and Git history are retained only as lessons for Bot2."
